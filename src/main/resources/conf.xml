<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

	<!-- 引用db.properties配置文件 -->
	<properties resource="db.properties" />

   <typeAliases>
   	<typeAlias type="com.mybatis.domain.User" alias="_User"/>
   </typeAliases>
   
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC" />
            <!-- 配置数据库连接信息 -->
            <dataSource type="POOLED">
                <property name="driver" value="${driver}" />
                <property name="url" value="${url}" />
                <property name="username" value="${name}" />
                <property name="password" value="${password}" />
            </dataSource>
        </environment>
    </environments>
    

	
	<mappers>
	<mapper resource="com/mybatis/mapping/userMapper.xml" />
	<mapper class="com.mybatis.mapping.UserMapperI" />
	<mapper resource="com/mybatis/mapping/orderMapper.xml"/>
	<mapper resource="com/mybatis/mapping/classMapper.xml" />
	<mapper resource="com/mybatis/mapping/classMapper2.xml" />
	<mapper resource="com/mybatis/mapping/userMapper2.xml" />
	</mappers>

	
	
	
</configuration>